import { StyleSheet } from 'react-native';

export const textInputBarStyles = StyleSheet.create({
    inputBarContainer: {
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-between',
        padding: 10,
    },
});